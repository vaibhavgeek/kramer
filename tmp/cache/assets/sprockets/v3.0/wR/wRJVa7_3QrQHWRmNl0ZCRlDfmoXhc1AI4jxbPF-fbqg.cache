I"*(function() {
  var Note;

  Note = (function() {
    function Note() {}

    Note.action = function(actions, ticket, article, ui) {
      return actions;
    };

    Note.perform = function(articleContainer, type, ticket, article, ui) {
      return true;
    };

    Note.articleTypes = function(articleTypes, ticket, ui) {
      var internal;
      internal = false;
      if (ui.permissionCheck('ticket.agent')) {
        internal = ui.Config.get('ui_ticket_zoom_article_note_new_internal');
      }
      articleTypes.push({
        name: 'note',
        icon: 'note',
        attributes: [],
        internal: internal,
        features: ['attachment']
      });
      return articleTypes;
    };

    return Note;

  })();

  App.Config.set('100-Note', Note, 'TicketZoomArticleAction');

}).call(this);
:ET