I"Ë	(function() {
  var Index,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Index = (function(superClass) {
    extend(Index, superClass);

    Index.prototype.requiredPermission = 'admin.overview';

    Index.prototype.header = 'Overviews';

    function Index() {
      var attribute, i, len, ref;
      Index.__super__.constructor.apply(this, arguments);
      ref = App.Overview.configure_attributes;
      for (i = 0, len = ref.length; i < len; i++) {
        attribute = ref[i];
        if (attribute.name === 'group_by') {
          attribute.options = App.Overview.groupByAttributes();
        }
      }
      new App.ControllerGenericIndex({
        el: this.el,
        id: this.id,
        genericObject: 'Overview',
        defaultSortBy: 'prio',
        pageData: {
          home: 'overviews',
          object: 'Overview',
          objects: 'Overviews',
          navupdate: '#overviews',
          notes: ['Overview are ...'],
          buttons: [
            {
              name: 'New Overview',
              'data-type': 'new',
              "class": 'btn--success'
            }
          ]
        },
        container: this.el.closest('.content'),
        large: true,
        dndCallback: (function(_this) {
          return function(e, item) {
            var id, items, j, len1, prio, prios;
            items = _this.el.find('table > tbody > tr');
            prios = [];
            prio = 0;
            for (j = 0, len1 = items.length; j < len1; j++) {
              item = items[j];
              prio += 1;
              id = $(item).data('id');
              prios.push([id, prio]);
            }
            return _this.ajax({
              id: 'overview_prio',
              type: 'POST',
              url: _this.apiPath + "/overviews_prio",
              processData: true,
              data: JSON.stringify({
                prios: prios
              })
            });
          };
        })(this)
      });
    }

    return Index;

  })(App.ControllerSubContent);

  App.Config.set('Overview', {
    prio: 2300,
    name: 'Overviews',
    parent: '#manage',
    target: '#manage/overviews',
    controller: Index,
    permission: ['admin.overview']
  }, 'NavBarAdmin');

}).call(this);
:ET